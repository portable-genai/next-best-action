name: CI

# Lint + format + type-check + tests + eval gate on the LOCAL profile (a WORKING offline
# stack), so CI needs NO Google Cloud credentials and NO google-cloud-* packages. It does need
# a short-lived, read-only GitHub App token for the private consent client kit. Integration
# tests are deselected. The contract + gcp-import tests additionally prove the onprem
# fail-fast family and the lazy-import GCP family with the SDKs absent.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  MKT_NBA_PROFILE: local

jobs:
  gate:
    name: ruff + mypy + pytest + eval (local)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Mint read-only token for private catalog commons
        id: commons-token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.CATALOG_COMMONS_APP_ID }}
          private-key: ${{ secrets.CATALOG_COMMONS_APP_PRIVATE_KEY }}
          owner: portable-genai
          repositories: consent-preference-kit

      - name: Configure private commons checkout
        env:
          CATALOG_COMMONS_TOKEN: ${{ steps.commons-token.outputs.token }}
        run: git config --global url."https://x-access-token:${CATALOG_COMMONS_TOKEN}@github.com/".insteadOf "https://github.com/"

      - name: Set up Python 3.14
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.14"
          cache: pip

      - name: Install (dev only, no GCP SDK)
        # Proves the domain + local profile install and run with no google-cloud-* deps.
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.lock && pip install -e . --no-deps

      - name: Ruff (lint)
        run: ruff check src tests

      - name: Ruff (format check)
        run: ruff format --check src tests

      - name: Mypy (type-check)
        run: mypy src

      - name: Pytest (unit + contract, integration deselected)
        run: pytest -m 'not integration' -q

      - name: Eval gate (A4 promotion gate)
        # Non-zero exit fails the gate; a release must not be promoted unless this is green.
        run: python eval/run_eval.py

      - name: API import + boot smoke (real app)
        # The run-api target must launch the real FastAPI app. Boot it and hit /healthz.
        run: |
          python -c "from next_best_action.api.app import app; assert app.title"
          uvicorn next_best_action.api.app:app --host 127.0.0.1 --port 8104 &
          for i in $(seq 1 30); do
            curl -fs http://127.0.0.1:8104/healthz && break || sleep 1;
          done
          curl -fs http://127.0.0.1:8104/healthz

      - name: Presenter demo self-test (all live steps)
        run: python scripts/demo_selftest.py

  ui-build:
    name: Next.js console builds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Node 20
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install + build the thin console
        working-directory: ui
        run: |
          npm ci
          npm run lint
          npm test
          NEXT_TELEMETRY_DISABLED=1 npm run build

      - name: Assert the built console can hydrate (nonce CSP)
        # Starts the BUILT server and reads the served document. A header assertion cannot see
        # this failure: the CSP is byte-identical when the page hydrates and when it does not.
        working-directory: ui
        run: npm run assert-hydratable

  supply-chain:
    name: dependency audit (pip-audit + npm audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Mint read-only token for private catalog commons
        id: commons-token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.CATALOG_COMMONS_APP_ID }}
          private-key: ${{ secrets.CATALOG_COMMONS_APP_PRIVATE_KEY }}
          owner: portable-genai
          repositories: consent-preference-kit
      - name: Configure private commons checkout
        env:
          CATALOG_COMMONS_TOKEN: ${{ steps.commons-token.outputs.token }}
        run: git config --global url."https://x-access-token:${CATALOG_COMMONS_TOKEN}@github.com/".insteadOf "https://github.com/"
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
          cache: pip
      # Audit the committed lockfiles so the result matches exactly what CI and the image
      # install. --no-deps: the locks are fully compiled, so audit those exact versions.
      - name: pip-audit (dev + gcp lockfiles)
        run: |
          python -m pip install --upgrade pip pip-audit
          pip-audit -r requirements-dev.lock --disable-pip --no-deps
          pip-audit -r requirements-gcp.lock --disable-pip --no-deps
      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "20"
      - name: npm audit (high severity fails the build)
        working-directory: ui
        run: npm audit --audit-level=high

  terraform-validate:
    name: Terraform format and offline validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: 1.13.0
      - name: Format and validate without a backend or credentials
        working-directory: infra/terraform
        run: |
          terraform fmt -check -recursive
          terraform init -backend=false -input=false
          terraform validate
