# Common-base practices audit

- **Repo:** `next-best-action`
- **Catalog id:** Mkt5 (package `next_best_action`, env prefix `MKT`, profile selector `MKT_NBA_PROFILE`)
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)
- **Authoritative source:** reconciled to the maintainer's cross-repository audit matrix, authoritative on verdicts.
- **Note:** Each check was re-run against the current tree with this repo's package/env/paths
  substituted. Mkt5 is a per-customer next-best-action / cross-sell agent (propensity plus
  deterministic eligibility, consent and ranking), so the tenancy and privacy checks C2, C3
  and C4 are load-bearing here (not N-A as on the broader-audience marketing repos) and are
  audited in full.

Applicability: Mkt5 ships a UI (`ui/`) and Terraform (`infra/terraform/`), so `[ui]` and
`[infra]` checks apply. It owns no web login flow (identity is an IAP-injected assertion in
secure mode, seeded dev personas locally, a client-IdP placeholder on-prem), so C8 is N-A.
**Load-bearing** checks (a FAIL breaks a shared catalog guarantee) are A1-A6, C1-C5, D1-D3
and E1.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `grep -rE "google\|fastapi\|httpx\|pydantic\|boto3\|azure" src/next_best_action/domain/` returns nothing. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | 11 ports under `ports/*.py`, including the Mkt6-backed `ConsentPort`; each is re-exported from `ports/__init__.py`, and contract tests enforce the registered set. |
| **A3** Swappable profiles by one config value `[all]` **(load-bearing)** | PASS | `MKT_NBA_PROFILE = local\|gcp\|platform\|onprem`; per-port `adapters:` map in `config/settings.yaml`; offline suite runs on `pip install -e ".[dev]"` with no GCP SDK. |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py::test_adapter_constructs_with_single_settings_arg` parametrises over `SDK_FREE_PROFILES` (onprem, local) x every port. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | `grep -n "^from google\|^import google" src/next_best_action/adapters/gcp/*.py` returns nothing; `agent/` has no top-level google import; `test_gcp_adapters_import_safe.py` imports the gcp modules SDK-free. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | `test_port_parity.py` proves structural parity, single-arg construction, runtime_checkable, onprem fail-fast, and that every registered port has onprem+local bindings. The reverse set-equality drift guard is now built: `test_port_protocols_matches_settings_adapters` fails on BOTH drift directions (a binding added to `settings.adapters` but absent from `PORT_PROTOCOLS`, and vice-versa). A behavioral-parity suite is added: `tests/contract/test_behavioral_parity.py` asserts the SDK-free `local` adapters are byte-identical across reruns (same frozen domain objects and `to_jsonable` payloads) for the recommendation, knowledge-base, guardrail, redaction and audit ports, that the `onprem` and scaffolded `platform` placeholders fail fast (`NotImplementedError`, never a silent wrong answer), and that the full pipeline is deterministic under `local` and fails fast under `onprem` on a profile change alone. `evaluation` (wired to Hrz4), `review_router` (real Hrz7 S2S) and `identity` (GCP IAP) are excluded from the placeholder list by design. |
| **A7** Kernel vs vertical split in the domain `[all]` | PASS | `domain/kernel.py` owns the provider-neutral `ThinkingLevel`; vertical `models.py` imports it from the kernel, never the reverse. It exports no customer, offer or recommendation aggregate; `test_kernel_boundary.py` enforces identity and that line. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PASS | `adapters/platform/remote_*.py` are 25 to 143 lines, marshalling only; guardrail, KB, audit, evaluation and registry each have a `platform` binding. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | `candidate_service.py`, `eligibility_service.py`, and `ranking_service.py` are pure, replayable, and unit-tested. Consent remains deterministic and cited in Mkt6 and is consumed through the versioned `ConsentPort`; Mkt5 does not own a second decision engine or BigQuery consent-table fallback. The LLM only writes the explanation over an already-fixed ranking. |
| **B2** Every claim carries a citation; empty grounding is a hard error `[agentic]` | PASS | Every `Recommendation`/`RankedOffer` carries `citations` built from offer/propensity/consent sources; an empty candidate set raises `NoCandidatesError` rather than recommending ungrounded. |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `RecommendationSet.requires_human_review: bool = True` default, set True unconditionally in the orchestrator; audit decision is `ESCALATED`; `review_safety >= 0.99` eval metric; `test_recommendation_pipeline.py` asserts `requires_human_review is True`. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | PASS | Ranking weights (`propensity_weight`, `value_weight`, `min_score`) live in `settings.yaml` and are threaded into `RankingService`; tests cover overrides. Consent policy is owned by Mkt6 or the adopter's preference centre, not exposed as a fail-open Mkt5 tuning knob. |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | All thirteen vocabularies are `StrEnum` (via the shared `hex-service-kit` commons): members ARE their wire values and `.value` call sites are unchanged. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | `api/schemas.py` `RecommendRequestModel` has no `actor` field; every route depends on `CurrentPrincipal` (`api/security.py` -> `IdentityPort.resolve`); `recommend(request, actor=principal.actor)`; unresolved identity is a 401. `test_api_identity.py` proves the audit actor is server-resolved. The IAP refusal is now split by whose failure it is, and the split is read at the API. In `adapters/gcp/iap_identity.py` the `MKT_NBA_IAP_AUDIENCE` check now runs FIRST, before the assertion header is read, and raises `IapAudienceUnconfiguredError`; the lazy google-auth import moved inside a `try` and raises `IapVerifierUnavailableError`. Both subclass `EndUserAuthUnavailableError` (declared in `ports/identity.py`, carrying `http_status`) and both answer 503, while an absent or blank assertion header stays a plain `IdentityError` and a bare 401 "authentication required" that tells a caller nothing they could forge with. `ctx.header(...)` is now `.strip()`ped, so a header a proxy or a deployment template rendered blank is refused as MISSING instead of reaching the algorithm pin. `api/security.py::get_principal` orders the `EndUserAuthUnavailableError` branch BEFORE the `IdentityError` branch, and the order is load-bearing: it is a subclass, so the reverse silently swallows it and answers the 401 the whole split exists to avoid. `tests/unit/test_iap_refusal_split.py` guards all of it. RED first, offline against the adapter exactly as it shipped: an unset audience with an assertion present gave `IdentityError: MKT_NBA_IAP_AUDIENCE is not configured; cannot verify IAP assertion`, while the operator's bare curl with no header gave `IdentityError: missing IAP assertion header; request did not pass through IAP`, so the deployment fault was reported only to callers who already had an assertion and both answered 401; a `"   "` header gave `IdentityError: assertion is not a compact JWS or JWE, so it carries no readable algorithm header`, a malformed token reported for an absent one; and with the `[gcp]` extra blocked, `ModuleNotFoundError: No module named 'google.auth.transport'` escaped `resolve` and `get_principal` for a bare 500 per request. The new module does not import against that tree at all; with only the two `Iap*Error` types injected and the old behaviour left standing it scored 8 failed / 3 passed, and reordering the two `except` branches on the fixed tree leaves 10 passed and fails `test_the_api_answers_a_deployment_failure_with_its_own_status_and_message` alone, 401 where 503 is owed. The fourth divergence from the `adapters/gcp/identity.py` family, the `principals` tuple, was investigated and deliberately NOT changed: this adapter grants `user:<subject>` and that family grants nothing, and it is this one that agrees with `hex_service_kit.federation.principal_from_iap_claims`, so aligning it would strip a verified identity of its own principal and widen nothing. The divergence is recorded in the test module rather than closed in the wrong direction. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | PASS | `Customer.tenant` partitions each customer, and `recommendation_service` runs a fail-closed deny immediately after the customer fetch (before any offer is built or audited): a customer whose tenant does not match the verified principal is refused, mapped to HTTP 403 (not 404, so existence is not confirmed). `recommend()` takes the verified `Principal`, not a free-text actor; a cross-tenant denial test proves the seeded `other-bank` persona is refused `demo-bank` customers, RED-before at the domain and HTTP layers. |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | PASS | The pipeline redacts then guardrail-screens at BOTH boundaries (`recommendation_service` calls `self._redact(raw_text)` before `guardrail.screen`), so PII never reaches the model. Every audit write goes through `_for_audit`, which pseudonymizes the internal customer key (SHA-256 to `cust#<12hex>`, including the consent-citation source ids that embedded it) and masks PII; `AuditEvent` fields are `redacted_prompt`/`redacted_response` (not raw `prompt`/`response`), written already-redacted into the WORM sink. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | PASS | The SG / JP / AU rows (D5's home markets, no HK by default), their checksum validators and the RE2-safe forms come from the shared, versioned **`pii-kit`** package (pinned to an exact SHA in `requirements-dev.lock`), NOT a local copy: the local regex redactor (`national_patterns_for` + `UNIVERSAL_PATTERNS`), the GCP DLP custom info types (`dlp_redaction._custom_info_types`, using `re2_pattern_for`) and the eval gate's leak check all read that ONE source, selected by `pii.jurisdictions` (`MKT_NBA_PII_JURISDICTIONS`). That selection is resolved once in `config.resolve_pii_jurisdictions` and fails closed: unset keeps the reviewed set, but a set-and-empty variable (`""`, `","`) and any code the pack has no rows for are both refused at load, before any adapter is built and before the DLP client's lazy SDK import; an empty tuple from an unguarded comma split would otherwise silently strip every national identifier out of the redactor, the DLP custom info types AND the eval leak check at once. Directly constructed adapters still degrade on an unknown-but-named code (the pack's documented contract), but both redaction adapters refuse an empty set. The package carries case-insensitive-SG / grouped-JP rows, checksum-gates the AU TFN and normalises `[\s-]`, avoiding the narrower `onprem-dlp`-derived forms (upper-case-only SG NRIC, bare-`\d{12}` JP My Number missing the grouped form, an unvalidated AU TFN). JP My Number and AU TFN keep their checksums, so an ordinary 9- or 12-digit reference is not masked out. D5 has no bank-account row, so there is no row-ordering hazard. Non-home-jurisdiction proof: the SG/JP/AU seed banking customers each carry that market's own fictional national id, and the gate scores 1.0 normally and drops to 0.5 (exactly the three PII-bearing cases) with redaction disabled. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | CORS is allowlisted and local binds loopback; unknown personas and cross-tenant access refuse. Consent permits only Mkt6's exact canonical `allowed` outcome: an unconfigured managed URL, transport failure, malformed answer, unknown state, or missing local fixture all suppress the offer. |
| **C6** Security-header baseline on every surface `[ui]` | PASS | The UI retains its single nonce-CSP emitter and hydration proof. API middleware now adds `nosniff`, `Referrer-Policy: no-referrer` and HSTS only for secure profiles; local HTTP remains usable. `test_api_security_headers.py` proves both branches. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | The managed Mkt6 consent client refuses a missing/non-HTTPS URL or audience and lazily mints a short-lived Google-signed ID token for the reviewed custom audience through Workload Identity. Mkt6 independently restricts Cloud Run invoker IAM and verifies the same runtime service-account caller in-app; Terraform carries no static consent secret. The Hrz4 evaluation call remains on the authenticated, https-only shared `PromotionGateClient`; remaining platform delegates are phase stubs. |
| **C8** Web login flow hardening `[ui]` | N-A | The repo owns no web login flow: identity is an IAP-injected assertion (secure), seeded dev personas (local) or a client-IdP placeholder (onprem); no OAuth/OIDC login is implemented here. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | `LocalAppendOnlyAuditAdapter` wraps the shared `hex_service_kit.audit.HashChainedAuditLog`: SHA-256 chain, UPDATE/DELETE triggers, JSONL export/restore, `verify_chain()`, honest-limits docstring. Proven by `tests/unit/test_audit_chain.py`. Events arrive already redacted (see C3). |
| **C10** No secret values in the repo `[all]` | PASS | `config/settings.yaml` names only env vars (`*_env` / `${VAR}`); a literal-secret grep over `config/` is clean. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | Committed locks pin all five catalog commons to 40-character commits, including private `consent-preference-kit@v0.0.1`; `make lock` recompiles and restores the tag-to-commit evidence header. The Dockerfile installs the managed lock. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | Base image digest-pinned, Actions SHA-pinned, dependabot present, `pip-audit` + `npm audit` hard CI gates. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | The gate is fully offline at runtime under `MKT_NBA_PROFILE=local` and needs no GCP or organization service credentials. The one thing that made this PARTIAL was source installation: `consent-preference-kit` was private, so the build minted a short-lived GitHub App token and the image build mounted it as a BuildKit secret, fail-closed, leaving two repository secrets that needed external provisioning. The kit has been public in `portable-genai` since 2026-08-22, so the commit-pinned `git+https` line resolves anonymously and all of that machinery is removed rather than left dormant: the Dockerfile installs with no secret mount, and no workflow mints a token. Proved by execution rather than by reading the diff: `docker build -t next-best-action:credential-free-proof .` completes with no BuildKit secret and no git credential configured, which is the build that could not have succeeded before. `docs/private-dependencies.md` is deleted; it documented a posture that no longer exists. |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | `Dockerfile`: multi-stage, `USER appuser` (uid 10001), `HEALTHCHECK` on `/healthz`, `EXPOSE 8104`, `MKT_NBA_PROFILE=gcp` set explicitly; the runtime stage copies only the venv (no build-essential). |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PASS | The existing region, per-service CMEK, no-SA-key, VPC-SC and WORM controls remain parameterised. CI and `make tf-validate` now run format, backend-free init and validation without credentials; local validation passed with google/google-beta 6.50.0. |
| **E1** Offline eval smoke guards merge; Hrz4 owns promotion `[agentic]` **(load-bearing)** | PASS | `eval/run_eval.py` has the `--mode smoke|gate` split via the shared `agent-eval-kit` scaffold; `remote_evaluation.py` re-based on the shared `PromotionGateClient` (registered bundle `mkt5-nba` and the n_examples passthrough both pinned by the respx contract test); gate mode refuses to run outside `MKT_NBA_PROFILE=platform|gcp`. The pii-kit wiring is untouched. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | `review_safety` and `pii_safety` both gate at 0.99. The PII metric runs the production redactor and combines the shared pack leak check with a pack-independent planted literal; disabling redaction makes each PII-bearing market case red. Checksum validators preserve precision for ambiguous JP/AU digit shapes, while SG identity/phone shapes are fail-safe boundary matches. No false-green gap remains. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | `eval/datasets/golden_recommendations.jsonl` header says "OBVIOUSLY-FICTIONAL synthetic"; the seed and `DEMO.md` describe fictional customers/offers/rules/consent. (The live-data sign-off warning lives in both `DEMO.md` and `COMPLIANCE.md`.) |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo` -> `scripts/demo.py` runs the real `RecommendationService` offline and renders static audit-first HTML; `make demo-server` -> `scripts/demo_server.py` is a live, presenter-controlled offline server; no cloud or API key. |
| **F2** Demo cannot rot silently `[all]` | PASS | Three layers, all executed. (1) Stable evidence hooks: the renderer and demo server emit `data-panel`, `data-nba-*`, `data-rec-*`, `data-suppressed-*`, `data-citation-source`, `data-review-required` and `data-artifact*` attributes carrying every load-bearing figure. (2) In-process, inside `make gate`: `scripts/demo_selftest.py` runs all six real local scenarios and renders every result. (3) Served stage, also inside `make gate`: the same script starts the REAL presenter server from `scripts/demo_server.py` on an ephemeral port, fetches the artifact index and every artifact page over HTTP, and compares each hook in the SERVED bytes against the value the RUNNING app computed. (4) Browser stage: `tests/browser/test_served_demo_ui.py` drives the same served pages through headless Chromium pinned by the `[demo]` extra (`playwright==1.62.0`), following the index's own links; `make demo-browser` runs it. RED proof: planting a stale hard-coded recommendation count and stripping one `data-panel` hook failed BOTH the served stage and the browser stage, each defect independently; restoring made both green. Scope note, tightened 2026-08-26 so a skip can no longer stand in for a run: the browser stage still skips when NOTHING said a browser was expected, so a day-one offline install (D3) gates without a browser download, and the served stage is unconditional either way. `DEMO_BROWSER_REQUIRED` is the three-state read that says one WAS expected, and then an absent `[demo]` extra or a failed launch FAILS instead of skipping; the fixture also passes `CHROME_PATH` into `chromium.launch`, so a runner shipping its own chromium is driven rather than ignored. RED first, observed on the pre-change file: with a real Chromium named by `CHROME_PATH`, no Playwright download present and `DEMO_BROWSER_REQUIRED=1`, the suite reported `2 skipped` and exit 0, proving both defects at once. On the changed file the same command is `2 passed`; dropping `CHROME_PATH` gives `2 errors` with `DEMO_BROWSER_REQUIRED is set, so a browser was expected here and this suite must not skip. no pinned browser binary available: BrowserType.launch: Executable doesn't exist`; making the `[demo]` extra unimportable gives a collection `ModuleNotFoundError` instead of a skip. Unset still skips (exit 0), and an EMPTY `DEMO_BROWSER_REQUIRED` refuses with `ConfiguredEmptyError`. The `.github/workflows/ci.yaml` browser step now sets the variable too, but that file states the contract rather than executing it: GitHub Actions is disabled in this repository, so the workflow has never run and the evidence above was executed locally. The hosted `portable-genai-ci` check is what actually gates a pull request. |
| **F3** Portability claim is executable `[all]` | PASS | `scripts/portability_demo.py` is the single offline gating executable. It runs the profile, port, identity, hash-chain audit and export/reload contracts, verifies the domain imports no Google SDK and states the hosted limits it cannot prove. `make gate` runs it. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | Root `AGENTS.md` declares `SPEC.md > ARCHITECTURE.md > COMPLIANCE.md > README.md > demo/FAQ`, with executable evidence resolving drift. The docs consistently name Mkt6 as the only consent authority and the shared client as the wire contract. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | COMPLIANCE.md present: P-01..P-13 + R1..R8 mapping with an Evidence column naming real files, plus an adopter-owned regulator crosswalk appendix. |
| **G3** Documented, mechanised fork path `[all]` | PASS | `docs/ADOPTING.md` states the core-vs-adopter-owned file boundary, the one-pass rebrand and the human-decision checklist (market/region, IdP, PII pack, ranking/consent policy, seed, eval golden set); `scripts/rename_fork.py` rewrites the package (`next_best_action`), CLI (`mkt-nba`), env prefix (`MKT_`) and resource stem (`next-best-action`) in one pass, previews with `--dry-run` (writes nothing, exit 0), and renames the package dir on `--yes`. |
| **G4** Retired `[all]` | N-A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | PASS | `docs/faq/` has an index `README.md` plus five role-specific FAQs (features, security, compliance, portability, adoption) tailored to Mkt5's real facts (per-customer, tenant-partitioned fail-closed authz, consent gating, eligibility/suitability ranking), each pointing at the sibling systems it integrates rather than duplicating them (Hrz1 guardrail, Hrz2 KB, Hrz3 registry, Hrz4 eval gate, Hrz5 audit, Hrz7 review console, Mkt6 marketing-compliance, Rsk6 on-prem DLP). |
| **G6** Contribution docs cover full extension touch list, enforced by a test `[all]` | PASS | `CONTRIBUTING.md` now has explicit adapter and new-port checklists covering Protocol, re-export, all profile bindings, `PORT_PROTOCOLS`, composition-root wiring, behavior tests and evidence docs. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS | An em-dash count (grep for U+2014) is 0 across every root and `docs/` markdown file; no mermaid blocks present. |

**Verdict counts:** 39 PASS, 0 PARTIAL, 0 FAIL, 2 N-A (41 checks).
The one PARTIAL this file used to carry was load-bearing D3, and its stated prerequisite was
`consent-preference-kit` being private. That prerequisite dissolved on 2026-08-22 when every kit
went public in `portable-genai`, so D3 is PASS and all 15 load-bearing checks are PASS. G4 is a
retired practice. On any conflict the matrix and the maintainer's per-system register are
authoritative for the current verdicts.

## Gaps carried to systems/

**Evidence index** (per-check detail is in the rows above; this is a summary):

- **Load-bearing:** C2 + C3 (object-level authz + boundary redaction); C4 (jurisdiction
  PII pack, re-based onto the shared `pii-kit`); D1 + D2 (supply-chain
  hardening); C5 + E1 (the hex-service-kit / agent-eval-kit commons); A6 (the
  set-equality drift guard `test_port_protocols_matches_settings_adapters` + the behavioral-parity
  suite `tests/contract/test_behavioral_parity.py`).
- **Non-load-bearing:** B5 (all vocabularies are `StrEnum`), C7 (outbound S2S bearer + https-only
  guard via the shared `_s2s.py`, applied to the two live calls: the Hrz4 eval client and the Hrz7
  review router) and C9 (hash-chained WORM audit via `hex_service_kit.audit.HashChainedAuditLog`).
- **Mandated artifacts:** `COMPLIANCE.md` (P-01..P-13 + R1..R8
  principle-to-control mapping with evidence pointers), `docs/runbook.md` and
  `docs/onprem-migration.md` exist, and `src/next_best_action/agent/` is a real ADK agent
  (`root_agent.py`, `tools.py`, `callbacks.py`, `agent_card.py` served at
  `/.well-known/agent-card.json`), not an empty package.
- **G2:** `COMPLIANCE.md` carries the full P-01..P-13 +
  R1..R8 mapping with an Evidence column naming real files AND the adopter-owned regulator crosswalk
  appendix.
- **Adopter docs:** G3 `docs/ADOPTING.md` +
  `scripts/rename_fork.py` (core-vs-adopter boundary, one-pass rebrand, dry-run verified to write
  nothing, human-decision checklist) and G5 `docs/faq/` (index + five role-specific FAQs
  referencing the sibling systems).

**Open gaps** (append to the row's audit note):

- **D3 (CLOSED 2026-08-23):** runtime and the complete gate were already offline and needed no
  cloud or organization service credential; what kept this open was a clean source install
  needing user-owned read access while `consent-preference-kit` was private, which was outside
  this repo's authority to change. The migration published every kit on 2026-08-22, so the
  external prerequisite is gone and the credential machinery it justified has been removed
  rather than left dormant.

F2 is PASS: the gated demo contract does not stop at the browserless
render. `scripts/demo_selftest.py` starts the real presenter server and reads every figure
back out of the served bytes through stable `data-*` hooks, and `make demo-browser` exercises
the same served pages in pinned headless Chromium. Both stages are proven able to go RED
against a planted stale figure and a stripped panel hook.
